
typedef u64 ulong;
typedef struct {
	ulong x;
	ulong y;
} vector2_t;

typedef struct {
	ulong x;
	ulong y;
	ulong z;
} vector3_t;

u64 myfunc(u64 a, u64 b){
	vector2_t vec;
	vec.x = 15;
	vec.y = 58;

	ulong* xptr = &vec.x;

	return vec.y * *xptr;
}

u64 foo(i32 X){
	i32 y = X + 45;
	ulong q;
	ulong w = 15;
	u64* ptr;

	if (X > 13){
		q = 5;
	} else y = y*10;

	while (y) {
		y = y - 1;
		w = w + 13;
	}

	u64 res = myfunc(w, q);
	
	return *ptr;
}

u64 main(i32 argc, u8** argv){
	foo(argc);
	return 42;
}
