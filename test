
typedef u64 ulong;
typedef struct {
	ulong x;
	ulong y;
} vector2_t;

typedef struct {
	ulong x;
	ulong y;
	ulong z;
} vector3_t;

void function(){
	u64 x = 0;
	while (x <= 10){
		x = x + 1;
	}
}

u64 myfunc(u64 a, u64 b){
	vector2_t vec;
	vec.x = 15;
	vec.y = 58;

	ulong* xptr = &vec.x;

	if (xptr){
		vec.x = vec.x+5;
	}

	return vec.y * *xptr;
}

u64 foo(i32 X){
	i32 y = X + 45;
	ulong q;
	ulong w = 15;
	u64* ptr;
	i64 sum = (i32)1542 + (u16)2688;
	function();

	if (X > 13){
		q = 5;
	} else y = y*10;

	while (y) {
		y = y - 1;
		w = w + 13;
	}

	u64 res = myfunc(w, q);
	
	return *ptr;
}

u32 main(i32 argc, u8** argv){
	foo(argc);
	return (f32)42.3;
}
