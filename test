i32 x;
i32 y = 15;

typedef u64 ulong;

u64 myfunc(u64 a, u64 b){
	return 15*3;
}

u64 foo(i32 X){
	i32 y = X + 45;
	ulong q;
	ulong w = 15;
	u64* ptr;

	if (X > 13){
		q = 5;
	} else y = y*10;

	while (y) {
		y = y - 1;
		w = w + 13;
	}

	u64 res = myfunc(w, q);
	
	return *ptr;
}
